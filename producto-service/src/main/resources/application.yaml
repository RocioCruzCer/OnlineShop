server:
  port: 8082 # <-- Puerto diferente para que no choque con usuarios

spring:
  application:
    name: producto-service
  datasource:
    url: jdbc:mysql://localhost:3306/onlineshop?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234567890 # Pon la misma contraseña de la vez pasada
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/